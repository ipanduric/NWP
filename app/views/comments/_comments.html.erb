<div align="center">
  <%= will_paginate comments %>
</div>

<% comments.each do |comment| %>
  <% render(partial: 'comments/comment', object: coment) %>
<% end %>

<div align="center">
  <%= will_paginate comments %>
</div>